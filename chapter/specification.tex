\chapter{系统建模和形式化描述}

本章旨在为分布式数据库的一致性模型构建一个统一且严谨的形式化描述框架。
首先，本章深入阐述了在分布式环境下进行一致性建模的必要性，详细分析了网络延迟和节点故障等因素对事务执行结果的潜在影响。
其次，本章精确定义了事务、操作、执行历史和快照等基本概念，并引入了可见性关系（Visibility Relation，VIS）和仲裁关系（Arbitration Relation，AR）这两个核心二元关系，从而构建了基于 $(\text{VIS}, \text{AR})$ 的规约框架。
紧接着，本章提出了六条关键的一致性公理，分别为内部一致性、外部一致性、传递可见性、前缀一致性、无冲突性和完全可见性，并给出了它们的形式化定义。
最后，本章基于上述公理体系，形式化定义了六个常用的隔离级别，按照一致性强度从弱到强依次为：读原子性（Read Atomic，RA）、因果一致性（Causal Consistency，CC）、并发快照隔离（Parallel Snapshot Isolation，PSI）、前缀一致性（Prefix Consistency，PC）、快照隔离（Snapshot Isolation，SI）和可串行化（Serialisability，SER）。本章还深入探讨了各隔离级别之间的偏序关系及其在防止特定数据异常方面的能力。


\section{一致性建模的必要性}
在理想的数据库系统中，我们期望事务的执行结果能够严格满足原子性（Atomicity）和隔离性（Isolation）的保证。这意味着，每一个事务都应当被视为一个独立且不可分割的操作单元，其执行过程不受任何其他并发事务的干扰。为实现这一目标，最直观的策略是在事务执行期间对所涉及的所有数据对象实施全局锁定，确保同一时刻仅允许一个事务进行修改。然而，这种强硬的锁定机制会对系统性能造成灾难性的打击，导致数据库难以应对高并发的请求压力，从而在实际应用中变得几乎不可用。通常，具备可串行化（Serializability）隔离级别的数据库虽然支持事务的并发执行以提升性能，但其底层依然通过锁机制或多版本并发控制（MVCC）等手段，确保并发执行的最终结果等价于某种串行执行顺序，以此来避免数据不一致异常的发生。

然而，一致性模型并非越强越好，而是一个需要在性能与正确性之间进行权衡（Trade-offs）的选择。越强的一致性模型通常意味着需要更多的节点间协调、更严格的同步机制、更长的等待时间以及更高的延迟，这往往会导致系统吞吐量的下降；反之，较弱的一致性模型则允许更多的异步操作、更多的本地优化空间、更高程度的并发以及更低的延迟，但其代价是可能出现更多的数据异常。性能和一致性如同站在天平的两端，为了支持高效的事务并发（Concurrency），现代分布式数据库往往必须向强一致性做出一定程度的妥协，即容忍某些特定类型的数据不一致异常（Anomalies）的发生。数据库所选择接受的异常类型集合，决定了其所采用的一致性模型（Consistency Model）。不同的一致性模型对事务执行施加了不同程度的约束，要求事务在执行前后均必须符合该模型的规范。

分布式数据库本质上是一类复杂的分布式系统，其数据和计算资源通常分散在不同的物理节点上，依赖网络进行通信。由于网络通信的延迟和故障往往是不可避免的，这导致分布式数据库在设计时，除了必须处理单机数据库在不同隔离级别下可能出现的逻辑异常外，还必须额外应对由网络延迟、分区和节点故障引发的一致性问题。

为直观说明这一点，我们考虑以下特定的执行情形：
\begin{equation}
    \begin{aligned}
        \operatorname{txn_1}    & \{\operatorname{write}(x, \text{post}); \operatorname{write}(y, \text{null})\} \\
        \| \operatorname{txn_2} & \{u=\operatorname{read}(x) ; \operatorname{write}(y, \text{comment})\}         \\
        \| \operatorname{txn_3} & \{v=\operatorname{read}(x) ; w=\operatorname{read}(y)\}
    \end{aligned}
\end{equation}
其中 $x$ 和 $y$ 代表两个独立的数据项，$u$、$v$、$w$ 分别表示读操作返回的结果。
在理想的串行执行假设下（例如：$txn_1$ 提交后 $txn_2$ 执行，$txn_2$ 提交后 $txn_3$ 执行），且不考虑分布式环境的复杂性，系统的状态转换应当是确定的，即应得出 $u=\text{post}$、$v=\text{post}$、$w=\text{comment}$ 的结果。
然而，在真实的分布式环境中，网络延迟可能导致更新操作的异步传播。例如，$txn_1$ 的更新可能因网络拥塞或故障而延迟到达 $txn_3$ 所在的节点，但却已成功到达 $txn_2$ 的节点并被观测到。这就可能导致 $txn_3$ 读取到一个不一致的状态组合：$u=\text{post}$（从 $txn_2$ 的行为推断 $x$ 已更新）、$v=\text{null}$（本地未收到 $x$ 的更新）、$w=\text{comment}$（读到了 $txn_2$ 的更新）。同理，若 $txn_1$ 相对于 $txn_3$ 完全延迟，而 $txn_2$ 已发生，$txn_3$ 甚至可能只看到 $v=\text{null}$、$w=\text{comment}$ 这种违背因果逻辑的部分更新。

一致性模型的定义，正是为了规范在分布式环境下允许出现的各类数据不一致情形。这份协议绝非是空洞的抽象约定，而是针对并发操作、副本同步、故障恢复、分片协作以及事务执行等所有可能诱发数据可见性异常的场景，所制定的一套形式化、可验证且可推理的行为约束体系。它精确界定了诸如以下关键问题：
\begin{itemize}
    \item \textbf{可见性条件}：写操作完成后，其他客户端、其他节点或并发事务在何种具体条件下可以观测到该写操作的结果；
    \item \textbf{全序关系}：来自不同客户端、不同分片或不同副本的多个操作，在全局时序中应当如何进行逻辑排序；
    \item \textbf{异常容忍度}：哪些具体的异常现象（如脏读、不可重复读、幻读、写偏序、副本滞后、读撕裂、部分提交等）是被模型允许的，而哪些则是被严格禁止的；
    \item \dots
\end{itemize}

归根结底，一致性模型的定义构成了数据库系统与应用程序开发者之间的一份神圣契约（Contract）。这份契约有效地实现了数据库底层复杂实现与上层应用逻辑的解耦。开发者可以根据具体的业务需求，从性能与一致性的权衡中选择最合适的模型；鉴于此，数据库系统则负责在该模型的约束下正确、高效地调度和执行事务，而应用开发者无需深究数据库内部的复杂运行机制。这种契约关系是分布式系统实现可规模化、可维护性、可替换性以及平滑升级的基础。若缺乏这份明确的契约，不同版本的数据库行为可能大相径庭；每一次网络波动或主从切换都可能带来不可预期的读/写结果，最终导致上层应用无法编写出稳定、可移植且可验证的业务逻辑。


\section{基本概念与建模}
从全局视角审视，考虑到事务在提交时间维度上客观存在的先后次序，我们使用符号 \History（History）来表示全局事务的执行历史。执行历史是一系列事务（Transaction，记为 \Txc 或 \Txc[x]）的有序集合。此外，\History 还需要明确标注其所遵循的隔离级别（Isolation Level）。本文将涉及六个主要的隔离级别，按一致性强度从高到低排列依次为：可串行化（Serialisability，SER）、快照隔离（Snapshot Isolation，SI）、前缀一致性（Prefix Consistency，PC）、并发快照隔离（Parallel Snapshot Isolation，PSI）、因果一致性（Causal Consistency，CC）以及读原子性（Read Atomic，RA）\cite{Schultz2015}。这些隔离级别的具体定义将在后文中详细阐述。

在微观层面，一个事务 \Txc 被定义为一系列操作（Operation）的执行序列。鉴于本研究主要针对键值（Key-Value）数据库，我们暂不考虑复杂的谓词逻辑操作。在此前提下，事务中的具体操作可抽象为以下几种基本类型：
\begin{itemize}
    \item \OpStart：事务开始操作，标志着一个新事务的启动。
    \item \OpCommit：事务提交操作，表示事务成功完成并将其更新持久化。
    \item \OpAbort：事务中止操作，表示事务执行失败，所有更改被回滚。
    \item \OpRead[k][v]：读操作，表示读取键 $k$ 的值，期望读取结果为 $v$。
    \item \OpWrite[k][v]：写操作，表示将键 $k$ 的值设置为 $v$。
\end{itemize}
其中，\OpRead 和 \OpWrite 操作的对象是键 $k$，所有键的集合记为 \Keys。

快照（Snapshot，记为 \Snapshot）是指数据库在特定时间戳上创建的只读一致性数据视图，它是实现分布式事务一致性的核心底层机制。本文主要探讨的是事务级别的快照技术。快照是多版本并发控制（Multi-Version Concurrency Control，MVCC）的基石，其核心思想是为每一个事务分配一个独立的快照视图，从而允许不同的事务并发执行而互不干扰，显著提升系统的并发处理能力和响应速度。在实现上，快照并不意味着对数据进行全量物理复制，而是通过确定版本时间戳（如读时间戳 readTimestamp、提交时间戳 commitTimestamp）并结合特定规则来实现：事务仅能看到该时间戳之前已提交的所有数据版本，而对之后发生的任何修改不可见。数据实体依然保留在原地，系统仅通过版本号进行“可见性过滤”。这种机制在大幅降低存储和处理开销的同时，为事务的高效并发提供了必要条件。形式化地，我们定义快照函数 $S$ 将键映射到其对应的值，表示在该快照视图下每个键的具体取值。

为了给出严谨的形式化表达，我们引入二元组来描述序列概念。以事务 \Txc 为例，一个事务 \Txc 被定义为二元组 $(O, po)$，其中 $O \subseteq Op$ 表示该事务包含的所有操作集合，$Op$ 为所有可能操作的有限非空全集。$po \subseteq O \times O$ 称为程序顺序（Program Order），它是一个严格的全序关系，用于规定事务内部各个操作的执行次序。同理，执行历史 \History 也采用类似的定义方式，其中 $SO$ 代表会话顺序（Session Order），也是一个严格的全序关系，用于表示不同事务之间的执行先后顺序。

以上概念的形式化表达如下：
\begin{equation}
    \begin{aligned}
        \mathcal{K} & = \{ x, y, \ldots\}, \quad \mathcal{V} = \{ \bot ,1, 2, \ldots, n \}                                                                                               \\
        Op          & = \{ \text{start}, \text{commit}, \text{abort} \} \cup \{ \text{read}(k, v), \text{write}(k, v) \mid k \in \mathcal{K}, v \in \mathcal{V} \}                       \\
        T           & = (O, po), O \subseteq Op, \quad  po \subseteq O \times O                                                                                                          \\
        \mathcal{H} & = (\mathcal{T}, SO, \text{level}), \quad \mathcal{T} = \{T_1, T_2, \ldots, T_n\},                                                                                  \\
                    & \quad SO \subseteq \mathcal{T} \times \mathcal{T}, \quad level \in \{\text{SER}, \text{SI}, \text{PC}, \text{PSI}, \text{CC}, \text{RA}\}  \triangleq  \mathcal{L} \\
        S           & : \mathcal{K} \rightharpoonup \mathcal{V},  S(k) = \bot \text{ if } k \notin \text{dom}(S)
    \end{aligned}
\end{equation}
其中，$\mathcal{K}$ 为键的集合，$\mathcal{V}$ 为值的集合（$\bot$ 表示空值），
$SO$ 为会话顺序（session order），表示同一会话内事务的执行顺序，
$S$ 为快照函数，将键映射到其对应的值。

此外，还有两个辅助运算符用于求得事务的读集合和写集合：
\begin{equation}
    \begin{aligned}
        \operatorname{RSet}(T) & = \{ k \in \mathcal{K} \mid \exists O_i = \text{read}(k, \cdot) \in T, \forall j < i, O_j \neq \text{write}(k, \cdot) \} \\
        \operatorname{WSet}(T) & = \{ k \in \mathcal{K} \mid \exists O \in T, O = \text{write}(k, v) \}
    \end{aligned}
\end{equation}
其中，$\operatorname{RSet}(T)$ 表示事务 $T$ 的读集合，即在本事务内被读取且在读取前未被本事务写入的键的集合；$\operatorname{WSet}(T)$ 表示事务 $T$ 的写集合，即被本事务写入的键的集合。

当两个事务的写集合交集不为空，即 $\operatorname{WSet}(T_1) \cap \operatorname{WSet}(T_2) \neq \emptyset$ 时，称这两个事务存在写冲突（write-conflict），记为 $T_1 \bowtie T_2$。

\subsection{VIS 和 AR 关系}
由于事务的处理结果是原子化的，而多个事务的处理是并发的，因此描述事务隔离级别的关键在于描述任意两个事务之间的关系。这些关系是事务集合笛卡尔积 $\mathcal{T} \times \mathcal{T}$ 的子集。我们重点考虑两个核心关系：$\text{VIS}, \text{AR} \subseteq \mathcal{T} \times \mathcal{T}$。

可见性关系（Visibility Relation）$\text{VIS} \subseteq \mathcal{T} \times \mathcal{T}$ 定义了一个事务的执行结果对另一个事务是否可见。
直观地说，如果 $(T_1, T_2) \in \text{VIS}$，也就是 $T_1 \xrightarrow{\text{VIS}} T_2$，则表示事务 $T_1$ 的所有写操作对事务 $T_2$ 可见，即 $T_2$ 在执行读操作时能够读取到 $T_1$ 写入的值。$\text{VIS}$ 是一个无环的偏序关系，即不存在 $T_1 \xrightarrow{\text{VIS}} T_2 \xrightarrow{\text{VIS}} T_1$ 的情况。可见性关系决定了事务读取数据时所看到的快照状态，是理解事务隔离级别的核心概念之一。从另一个角度看，两个事务之间不存在可见性关系意味着它们是并发的。

仲裁关系（Arbitration Relation）$\text{AR} \subseteq \mathcal{T} \times \mathcal{T}$ 定义了事务之间写操作的全局顺序。当 $(T_1, T_2) \in \text{AR}$，即 $T_1 \xrightarrow{\text{AR}} T_2$ 时，表示事务 $T_1$ 的写操作在逻辑上先于事务 $T_2$ 的写操作，换言之，$T_2$ 的写入结果会覆盖 $T_1$ 对相同键的写入。$\text{AR}$ 是一个全序关系，即对于任意两个事务 $T_1$ 和 $T_2$，要么 $T_1 \xrightarrow{\text{AR}} T_2$，要么 $T_2 \xrightarrow{\text{AR}} T_1$。仲裁关系确定了并发写操作的最终顺序，解决了写冲突问题。


在进一步讨论这两个关系之前，我们需要明确一个重要的性质：$\text{VIS} \subseteq \text{AR}$，即可见性关系是仲裁关系的子集。这个性质并非偶然，而是保证数据库系统正确性的必要条件。

直观地理解,如果事务$T_1$的写入对事务$T_2$可见(即$T_1 \xrightarrow{\text{VIS}} T_2$),那么在逻辑上,$T_1$的写操作必然先于$T_2$的写操作被系统接受(即$T_1 \xrightarrow{\text{AR}} T_2$)。这是因为,$T_2$能够读取到$T_1$的写入结果,说明$T_1$已经成功提交,并且其写入的值没有被后续的其他事务覆盖。因此,在全局的写操作顺序中,$T_1$必然排在$T_2$之前。

从另一个角度看，$\text{VIS}$是一个偏序关系（partial order），它描述了事务之间已经确定的因果依赖关系。而$\text{AR}$是一个全序关系（total order），它为所有事务的写操作定义了一个全局的先后顺序。$\text{AR}$的作用是在$\text{VIS}$的基础上进行扩展：对于已经存在$\text{VIS}$关系的事务对，$\text{AR}$必须遵守这个既定的顺序；对于没有$\text{VIS}$关系的并发事务，$\text{AR}$需要"仲裁"出一个确定的顺序，以解决写冲突。

在实际的数据库系统实现中,$\text{AR}$通常通过时间戳(Timestamps)机制来实现。如果$T_1 \xrightarrow{\text{VIS}} T_2$,意味着$T_2$在执行时已经观察到了$T_1$的更新,或者$T_2$是在$T_1$提交之后才开始执行的。因此,$T_2$获取的时间戳必然晚于$T_1$的时间戳,从而保证$T_1 \xrightarrow{\text{AR}} T_2$。

这个性质$\text{VIS} \subseteq \text{AR}$的重要意义在于：
\begin{itemize}
    \item \textbf{保证因果一致性}：如果一个事务观察到了另一个事务的结果，那么在全局顺序中，前者必须排在后者之前，这确保了因果关系的正确传递。
    \item \textbf{确保数据正确覆盖}：对于有可见性关系的事务，后来者的写入应当覆盖前者的写入，避免出现"时光倒流"的异常情况。
    \item \textbf{提供一致性基础}：这个性质是定义和实现各种隔离级别的基础，不同的隔离级别可以在此基础上施加额外的约束。
\end{itemize}

因此，$\text{VIS} \subseteq \text{AR}$不仅是一个数学性质，更是分布式数据库系统保证数据一致性和正确性的核心机制。

这两个关系共同构成了$(\text{VIS}, \text{AR})$规约框架，为分布式数据库的一致性模型提供了统一的形式化描述基础。通过对VIS和AR关系施加不同的约束条件，可以精确地刻画不同隔离级别的语义。例如，在可串行化（Serializable）隔离级别下，$\text{VIS}$和$\text{AR}$系需要满足严格的约束，保证事务的执行结果等价于某个串行执行顺序；而在较弱的隔离级别下，这些约束可以适当放松，从而允许更多的并发执行，提高系统性能。

此外，我们约定 $\operatorname{VIS^{-1}}(T)$ 表示对事务 $T$ 可见的所有事务的集合，即 $\operatorname{VIS^{-1}}(T) = \{ T' \in \mathcal{T} \mid T' \xrightarrow{\text{VIS}} T \}$。其他关系的逆集合也做类似约定。我们还定义 $\operatorname{WriteTx}_k$ 表示对键 $k$ 有写操作的事务集合，并用 $T \Vdash \operatorname{op}$ 表示事务 $T$ 包含操作 $\operatorname{op}$。

基于以上提供的符号，我们可以定义抽象执行（Abstract Execution）为$\mathcal{X} = (\mathcal{T}, $SO$, \text{level}, \text{VIS}, \text{AR})$，因为$\mathcal{H}$ 中的 $\mathcal{T}$、$SO$ 和 $\text{level}$ 已经定义过了，我们可以简写为 $\mathcal{X} = (\mathcal{H}, \text{VIS}, \text{AR})$。

\section{异常与一致性公理}

\subsection{数据一致性异常}
由于分布式环境下的网络延迟和节点故障，分布式数据库可能出现多种数据一致性异常。在介绍一致性公理之前，我们利用前文构建的 $(\text{VIS}, \text{AR})$ 框架，对几种典型的异常进行形式化描述。

\subsubsection{因果关系违背 (Causality Violation)}
因果一致性要求：若事务观察到某项更新，则也必须观察到该更新的因果前驱。如图~\ref{fig:anomaly-causality}所示，\Txc[3] 观察到了 \Txc[2] 的评论，但没有观察到评论所针对的 \Txc[1] 的帖子。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        % 事务框
        \node[txn] (t1) {\Txc[1]: write(x, post) $\xrightarrow{po}$ write(y, empty)};
        \node[txn, below=1.2cm of t1] (t2) {\Txc[2]: read(x, post) $\xrightarrow{po}$ write(y, comment)};
        \node[txn, below=1.2cm of t2] (t3) {\Txc[3]: read(x, empty) $\xrightarrow{po}$ read(y, comment)};

        % 关系线
        % VIS (右侧) - 增大弧度，使用 east 锚点避免遮挡
        \draw[vis] (t1.east) to [bend left=60] node[right, blue] {VIS} (t2.east);
        \draw[vis] (t2.east) to [bend left=60] node[right, blue] {VIS} (t3.east);

        % AR (中间及左侧) - 补全标签
        \draw[ar] (t1) -- node[left, red] {AR} (t2);
        \draw[ar] (t2) -- node[left, red] {AR} (t3);
        \draw[ar] (t1.west) to [bend right=60] node[left, red] {AR} (t3.west);
    \end{tikzpicture}
    \caption{因果关系违背示例}
    \label{fig:anomaly-causality}
\end{figure}

\subsubsection{破碎读 (Fractured Reads)}
事务观察到了另一事务的部分更新。如图~\ref{fig:anomaly-fractured} 所示，\Txc[2] 读到了 Alice 关注了 Bob（由 \Txc[1] 写入），但却没读到 Bob 关注了 Alice（同样由 \Txc[1] 写入）。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        \node[txn] (t1) {\Txc[1]: write($x_A$, Bob) $\xrightarrow{po}$ write($x_B$, Alice)};
        \node[txn, right=2.0cm of t1] (t2) {\Txc[2]: read($x_A$, Bob) $\xrightarrow{po}$ read($x_B$, empty)};

        \draw[vis] (t1) -- node[above, blue] {VIS} (t2);
    \end{tikzpicture}
    \caption{破碎读示例}
    \label{fig:anomaly-fractured}
\end{figure}

\subsubsection{脏读 (Dirty Read)}
脏读允许事务读取到其他并发事务尚未提交的中间状态。如图~\ref{fig:anomaly-dirtyread} 所示，\Txc[1] 在执行过程中修改了 $x$ 的值，该值在 \Txc[1] 提交之前被 \Txc[2] 读取。这意味着 \Txc[2] 观察到了 \Txc[1] 的中间状态（Uncommitted Data），不管 \Txc[1] 后续是提交还是中止，这种现象都违背了隔离性原则。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        % T1
        \node (t1_op1) {write(x, 10)};
        \node[right=1.2cm of t1_op1] (t1_op2) {$\dots$};
        \draw[->] (t1_op1) -- node[above, font=\footnotesize] {$po$} (t1_op2);

        \node[left=0.2cm of t1_op1] (l1) {\Txc[1]:};
        % 手动绘制虚线框表示事务范围，体现"打破框体"
        \draw[dashed, gray] ($(l1.north west)+(-0.1,0.25)$) rectangle ($(t1_op2.south east)+(0.2,-0.25)$);

        % T2
        \node[below=1.5cm of t1_op1] (t2_op1) {read(x, 10)};
        \node[left=0.2cm of t2_op1] (l2) {\Txc[2]:};
        \draw[dashed, gray] ($(l2.north west)+(-0.1,0.25)$) rectangle ($(t2_op1.south east)+(0.2,-0.25)$);

        % VIS
        \draw[vis] (t1_op1) -- node[right, blue] {VIS} (t2_op1);
    \end{tikzpicture}
    \caption{脏读示例}
    \label{fig:anomaly-dirtyread}
\end{figure}

\subsubsection{更新丢失 (Lost Update)}
两个事务并发读取同一数据并基于原值进行更新，其中一个事务的提交覆盖了另一个事务的修改。如图~\ref{fig:anomaly-lostupdate} 所示，\Txc[2] 的更新被 \Txc[1] 的后续写入（在 AR 顺序上）覆盖，或者反之，导致其中一个更新虽然提交但实际上被丢弃。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        \node[txn] (t1) {\Txc[1]: read(acct, 0) $\xrightarrow{po}$ write(acct, 50)};
        \node[txn, below=0.8cm of t1] (t2) {\Txc[2]: read(acct, 0) $\xrightarrow{po}$ write(acct, 25)};
        \node[txn, right=2.0cm of t1, yshift=-0.4cm] (t3) {\Txc[3]: read(acct, 25)};

        \draw[ar] (t1) -- node[left, red] {AR} (t2);
        \draw[vis] (t2) to [bend right=30] node[below, blue] {VIS} (t3);
        \draw[vis] (t1) to [bend left=40] node[above, blue] {VIS} (t3);
    \end{tikzpicture}
    \caption{更新丢失示例}
    \label{fig:anomaly-lostupdate}
\end{figure}

\subsubsection{长叉异常 (Long Fork)}
长叉异常允许两个并发事务以互斥的顺序观察彼此。如图~\ref{fig:anomaly-longfork} 所示，\Txc[3] 认为 \Txc[1] 发生在 \Txc[2] 之前，而 \Txc[4] 认为 \Txc[2] 发生在 \Txc[1] 之前。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        \node[txn] (t1) {\Txc[1]: write(x, post1)};
        \node[txn, below=1cm of t1] (t2) {\Txc[2]: write(y, post2)};
        \node[txn, right=1.5cm of t1] (t3) {\Txc[3]: read(x, post1) $\xrightarrow{po}$ read(y, empty)};
        \node[txn, right=1.5cm of t2] (t4) {\Txc[4]: read(x, empty) $\xrightarrow{po}$ read(y, post2)};

        \draw[vis] (t1) -- node[above, blue] {VIS} (t3);
        \draw[vis] (t2) -- node[above, blue] {VIS} (t4);
    \end{tikzpicture}
    \caption{长叉异常示例}
    \label{fig:anomaly-longfork}
\end{figure}

\subsubsection{写偏斜 (Write Skew)}
两个事务读取重叠的数据集，但修改不相交的数据项。如图~\ref{fig:anomaly-writeskew} 所示，虽然每个事务独立看都符合约束（总和 $>100$），但并发执行后导致约束失效（总和变为 $-80$）。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}
        \node[txn] (t1) {\Txc[1]: read(acct1, 60) $\xrightarrow{po}$ read(acct2, 60) $\xrightarrow{po}$ write(acct1, -40)};
        \node[txn, below=0.8cm of t1] (t2) {\Txc[2]: read(acct1, 60) $\xrightarrow{po}$ read(acct2, 60) $\xrightarrow{po}$ write(acct2, -40)};
    \end{tikzpicture}
    \caption{写偏斜示例}
    \label{fig:anomaly-writeskew}
\end{figure}

\subsection{一致性公理}
现在我们已经了解了分布式数据库中可能出现的数据一致性异常，接下来介绍用于定义一致性模型的公理。这些公理基于前文介绍的 $(\text{VIS}, \text{AR})$ 框架，精确刻画了不同隔离级别下允许和禁止的异常类型。

所有一致性模型必须遵守以下两个基础公理：
\begin{itemize}
    \item \textbf{Int（内部一致性公理，Internal Consistency）}：
          内部一致性公理确保事务内部的读写行为符合串行语义。例如，一个事务如果先写了某个对象再读该对象，必须能读到自己刚写入的值。这条公理保证了单事务视角的逻辑正确性。
    \item \textbf{Ext（外部一致性公理，External Consistency）}：
          外部一致性公理定义了事务如何读取其他事务写入的值。它规定，对于本事务未写入过的对象的读取（外部读取），其值取决于所有对它可见的事务中，在 AR 顺序下最新的那个事务写入的值。这条公理用于防止断裂读取（Fractured Reads）和脏读（Dirty Reads）。
\end{itemize}

这两个公理是所有一致性模型的基础，如果一个数据库系统不支持内部一致性和外部一致性公理，那么对事物的原子性和隔离性都无法保证。这样的数据库对事务不存在任何支持，无法满足应用程序对事务的基本需求。除此之外，还有四个额外公理可用于进一步刻画一致性模型的语义：
\begin{itemize}
    \item \textbf{TransVis（传递可见性，Transitive Visibility）}：
          传递可见性公理确保如果事务 $T_1$ 对事务 $T_2$ 可见，而 $T_2$ 又对事务 $T_3$ 可见，那么 $T_1$ 对 $T_3$ 也必须可见。这条公理用于防止对违背因果给厝（Causality Violation）。例如，Alice 发帖，Bob 看到后评论。如果 Charlie 看到了 Bob 的评论，根据传递性，Charlie 也必须能看到 Alice 的原帖。
    \item \textbf{Prefix（前缀一致性，Prefix Consistency，PC）}：
          前缀一致性公理确保如果事务 $T_2$ 对 $T_1$ 可见， 那么在 $\text{AR}$ 顺序下排在 $T_2$ 之前的所有事务也必须对 $T_1$ 可见。换言之，事务所看到的历史必须是一个完整的、没有空洞的前缀（Snapshot）。 这条公理用于防止长分叉（Long Forks），即防止两个并发事务各自看到不同的、不兼容的历史版本。
    \item \textbf{NoConflict（无冲突）}：
          无冲突公理确保如果两个事务存在写冲突，则它们之间必须存在可见性关系，不能彼此并发而互不可见。这条公理用于防止丢失更新（Lost Update），即防止某个事务的更新被另一个并发事务直接覆盖。
    \item \textbf{TotalVis（完全可见性，Total Visibility）}：
          完全可见性公理要求 $\text{VIS}$ 关系必须是一个全序关系，即对于任意两个事务 $T_1$ 和 $T_2$，
          要么 $T_1 \xrightarrow{\text{VIS}} T_2$，要么 $T_2 \xrightarrow{\text{VIS}} T_1$。这意味着所有事务都在一条单一的时间线上，每个事务都能看到它之前的所有事务。 这是最强的公理。
\end{itemize}
其中，TransVis 和 Prefix 两条公理是对 $\text{VIS}$ 关系的限制和强化，而 NoConflict 和 TotalVis 两条公理主要聚焦于写冲突的解决方案。

以上公理的形式化表达如下：
\begin{equation}
    \label{eq:consistency}
    \begin{aligned}
        \mathsf{Int}(T)        & \equiv \forall r \in O.\ \forall x, v.\ \bigl(r = \operatorname{read}(x, v) \land po_x^{-1}(r) \neq \emptyset\bigr)                                                                                   \\
                               & \qquad\Rightarrow \max\nolimits_{po}\bigl(po_x^{-1}(r)\bigr) = \operatorname{write}(x, v),                                                                                                            \\[0.4em]
        \mathsf{Ext}(T)        & \equiv \forall x, v.\ T \Vdash \operatorname{read}(x, v) \Rightarrow \max\nolimits_{\text{AR}}\bigl(\operatorname{VIS^{-1}}(T) \cap \operatorname{WriteTx}_x\bigr) \Vdash \operatorname{write}(x, v), \\[0.4em]
        \mathsf{TransVis}(T)   & \equiv \forall T', S \in \mathcal{T}.\ T' \xrightarrow{\text{VIS}} S \land S \xrightarrow{\text{VIS}} T \Rightarrow T' \xrightarrow{\text{VIS}} T,                                                    \\[0.4em]
        \mathsf{Prefix}(T)     & \equiv \forall T', S \in \mathcal{T}.\ T' \xrightarrow{\text{AR}} S \land S \xrightarrow{\text{VIS}} T \Rightarrow T' \xrightarrow{\text{VIS}} T,                                                     \\[0.4em]
        \mathsf{NoConflict}(T) & \equiv \forall T' \in \mathcal{T}.\ T' \bowtie T \land T' \xrightarrow{\text{AR}} T \Rightarrow T' \xrightarrow{\text{VIS}} T,                                                                        \\[0.4em]
        \mathsf{TotalVis}(T)   & \equiv \forall T' \in \mathcal{T}.\ T' \xrightarrow{\text{AR}} T \Rightarrow T' \xrightarrow{\text{VIS}} T.
    \end{aligned}
\end{equation}
其中，$po_x^{-1}(r)$ 表示在程序顺序 $po$ 下排在读操作 $r$ 之前且操作对象为键 $x$ 的所有操作的集合，$\max_{po}$ 表示在程序顺序下取最后一个操作，$\max_{\text{AR}}$ 表示在仲裁顺序下取最新的事务。

\section{隔离级别}
基于上述一致性公理的定义，接下来我们讨论各个隔离级别的特点。为了在一致性、可扩展性和可用性之间取得平衡，现代数据库系统提供了一系列不同强度的隔离级别。这些级别根据其提供的可见性保证和所能防止的异常现象，构成了一个从最弱的“原子读”到最强的“可串行化”的层级体系。
\\

\noindent\textbf{读原子性（Read Atomic，RA）}是最弱的一致性级别，仅满足 $\mathsf{Int}$ 和 $\mathsf{Ext}$ 两个公理。它只保证事务的更新以原子方式对其他事务可见，即要么看到一个事务的全部写入，要么完全看不到该事务。RA 不对事务之间的可见性关系施加额外约束，因此几乎所有其他异常都可能发生。例如图~3(a) 中的因果违背（Causality Violation），$T_2$ 能够看到 $T_1$ 的写入，$T_3$ 能够看到 $T_2$ 的写入，但 $T_3$ 却看不到 $T_1$ 对同一对象的更新，打破了直观上的因果链。

\noindent\textbf{因果一致性（Causal Consistency，CC）}在读原子性的基础上增加了 $\mathsf{TransVis}$ 公理，要求可见性关系 VIS 必须具有传递性。也就是说，如果 $T_3$ 看到了 $T_2$，而 $T_2$ 又看到了 $T_1$，那么 $T_3$ 必须也能看到 $T_1$。因此 CC 能够消除因果违背异常。但由于 CC 并不禁止对同一对象的并发写，丢失更新（Lost Update）异常仍然被允许。

\noindent\textbf{并发快照隔离（Parallel Snapshot Isolation，PSI）}在因果一致性的基础上增加了 $\mathsf{NoConflict}$ 公理。该公理要求对同一对象进行写入的两个事务不能彼此并发而互不可见，它们之间必须通过 VIS 确定先后顺序。直观地说，并发写冲突必须被仲裁，不能惄无声息地丢掉某个更新。因此 PSI 能够排除丢失更新异常。不过，PSI 仍允许不同事务在不同节点上看到不一致的历史前缀，从而产生长分叉（Long Fork）异常。

\noindent\textbf{前缀一致性（Prefix Consistency，PC）}从另一个维度增强因果一致性。它采用 $\mathsf{Prefix}$ 公理来约束可见历史，该公理蕴含了 $\mathsf{TransVis}$，要求如果事务 $T_1$ 看到了 $T_2$，那么在 AR 顺序中排在 $T_2$ 之前的所有事务都必须对 $T_1$ 可见。换言之，每个事务所看到的历史必须是全局 AR 顺序的一段前缀。因此，长分叉异常在 PC 下会被禁止。但由于 PC 并不禁止在同一快照上对不同对象进行并发写入，写偏斜（Write Skew）异常在 PC 中仍可能发生。

\noindent\textbf{快照隔离（Snapshot Isolation，SI）}可看作并发快照隔离与前缀一致性的合流点。它一方面继承了 $\mathsf{Prefix}$，要求所有事务在 AR 序列的某个前缀上执行；另一方面继承了 $\mathsf{NoConflict}$，禁止对同一对象的并发写冲突。因此，SI 同时防止了丢失更新和长分叉两类异常。SI 在一致性和性能之间取得了较好的平衡，因此被许多数据库系统支持或采用。常见数据库系统都有类似的隔离级别实现。然而，由于 SI 仍允许事务在同一快照上对不同对象进行更新，写偏斜异常仍然可能出现。

\noindent\textbf{可串行化（Serialisability，SER）}通过 $\mathsf{TotalVis}$ 公理将可见性关系强化为全序关系。对于任意两个事务 $T_1$ 和 $T_2$，要么 $T_1 \xrightarrow{\text{VIS}} T_2$，要么 $T_2 \xrightarrow{\text{VIS}} T_1$，不再允许它们在抽象执行层面真正并发。从结果上看，所有事务的执行效果等价于按照某个串行顺序依次执行。因此，SER 排除了所有前述异常，包括写偏斜，是图~\ref{fig:isolevels} 中最强的一致性级别。SER 提供了最直观的编程模型，开发者可以假设每个事务是在完全隔离的环境中执行的，无需考虑复杂的并发场景。但其代价是较高的性能开销，因为系统需要通过加锁或乐观并发控制等机制来确保事务的串行化执行。可以补充的是，因为 $VIS \subseteq AR$，强调 VIS 上的全序关系，也是强调 AR 上的全序关系。

表~\ref{tab:consistency-models} 从弱一致性级别到强一致性级别，列出了每个隔离级别需要满足的公理以及能够防止或允许的异常。表中 $\checkmark$ 表示该隔离级别允许该异常发生，$\times$ 表示能够防止该异常。
\begin{table}[htbp]
    \centering
    \caption{一致性模型及其所满足的公理与异常}
    \label{tab:consistency-models}
    \small
    \setlength{\tabcolsep}{4pt} % 减小列间距
    \begin{tabular}{c l c c c c c}
        \hline
        $\Phi$ & Axioms                                            & \makecell{Fractured                                              \\reads} & \makecell{Causality\\violation} & \makecell{Lost\\update} & \makecell{Long\\fork} & \makecell{Write\\skew} \\
        \hline
        RA     & $\mathsf{Int},\ \mathsf{Ext}$
               & $\times$                                          & $\checkmark$        & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
        \midrule
        CC     & $\mathsf{Int},\ \mathsf{Ext},\ \mathsf{TransVis}$
               & $\times$                                          & $\times$            & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
        \midrule
        PSI    & \makecell[l]{$\mathsf{Int},\ \mathsf{Ext},$                                                                          \\$\mathsf{TransVis},\ \mathsf{NoConflict}$}
               & $\times$                                          & $\times$            & $\times$     & $\checkmark$ & $\checkmark$ \\
        \midrule
        PC     & $\mathsf{Int},\ \mathsf{Ext},\ \mathsf{Prefix}$
               & $\times$                                          & $\times$            & $\times$     & $\times$     & $\checkmark$ \\
        \midrule
        SI     & \makecell[l]{$\mathsf{Int},\ \mathsf{Ext},$                                                                          \\$\mathsf{Prefix},\ \mathsf{NoConflict}$}
               & $\times$                                          & $\times$            & $\times$     & $\times$     & $\times$     \\
        \midrule
        SER    & $\mathsf{Int},\ \mathsf{Ext},\ \mathsf{TotalVis}$
               & $\times$                                          & $\times$            & $\times$     & $\times$     & $\times$     \\
        \hline
    \end{tabular}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.4\textwidth]{figures/isolevels.pdf}
    \caption{隔离级别间的偏序关系}
    \label{fig:isolevels}
\end{figure}

从图~\ref{fig:isolevels} 和表~\ref{tab:consistency-models} 给出了每个隔离级别所允许和阻止的异常，并基于对异常的选择，讨论了各个隔离级别之间的关系，用偏序关系表示，本文讨论的六个隔离级别在一致性约束上形成了一条从弱到强的偏序链。

\section{本章小结}
本章系统地构建了分布式数据库一致性模型的形式化描述框架。
首先，本章阐述了在分布式环境中建立一致性模型的必要性，指出了网络延迟、节点故障等因素对事务一致性的挑战，以及一致性模型作为数据库与应用契约的重要性。
其次，本章形式化定义了事务、执行历史和快照等基本概念，并基于可见性关系 ($\text{VIS}$) 和仲裁关系 ($\text{AR}$) 构建了 ($\text{VIS}, \text{AR}$) 规约框架，确立了 $\text{VIS} \subseteq \text{AR}$ 的基本一致性约束。
在此基础上，本章归纳并定义了六条核心的一致性公理：内部一致性、外部一致性、传递可见性、前缀一致性、无冲突性和完全可见性，并利用这些公理精确刻画了因果违背、破碎读、脏读、更新丢失、长叉异常和写偏斜等常见的数据一致性异常。
最后，本章利用上述公理体系，对读原子性 (RA)、因果一致性 (CC)、并发快照隔离 (PSI)、前缀一致性 (PC)、快照隔离 (SI) 和可串行化 (SER) 这六种隔离级别进行了严格的形式化定义，并构建了它们之间的一致性强度偏序关系。这一框架为后续章节中分布式事务处理协议的设计与分析提供了坚实的理论基础。




