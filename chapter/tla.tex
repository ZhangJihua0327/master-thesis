\chapter{支持混合隔离级别机制的\TLAPlus 建模与验证}

\section{事务操作语义的\TLAPlus 表达}

第二章已经介绍了 \TLAPlus 与 TLC 的基本概念。本章在此基础上进一步面向本文提出的事务模型，给出事务操作语义的 \TLAPlus 表达方式。形式化规约仍采用``初始状态 $Init$ + 状态转移 $Next$ + 安全断言''的组织范式，但其中涉及的状态变量、动作定义与约束条件均围绕混合隔离级别事务的执行过程展开。

这里的建模目标与数据库内核模拟不同。本节不重建锁管理器、日志重放或预写日志（WAL）等存储引擎部件，只描述事务在不同隔离级别下能够看到哪些历史版本、能够写入哪些键、以及提交时能否通过并发校验。这样得到的状态空间足以承载乐观并发控制（OCC）下混合隔离级别的全部判定，又不至于被存储工程细节撑大。

在此基础上，事务执行过程被抽象成一个离散状态机。状态空间分为四层：持久化层由全局键值存储 \texttt{store} 及其版本历史组成，对应数据库对外可见的状态；事务局部层包含事务私有的写缓冲 \texttt{tx\_buffer}、读快照 \texttt{tx\_snapshot} 与检查集 \texttt{tx\_checkset}，承担本地修改的隔离与并发校验输入的积累；操作历史层由 \texttt{op\_seq} 顺序记录每个事务的读写动作，是公理检查的直接输入；会话元信息层包含 \texttt{tx\_status}、\texttt{tx\_commit\_time} 与客户端依赖 \texttt{client\_dep} 等变量，在跨事务可见性判定时携带会话语义。这样划分后，任意时刻的系统可见状态都有对应的形式化表达，公理检查可以在同一组变量上展开\footnote{项目模块代码提供在 \url{https://github.com/ZhangJihua0327/mil-tla-verify} 仓库中。}。

本章把操作语义的建模拆成``动作定义''与``公理约束''两步：前者刻画状态如何在事务生命周期中演化，后者判定演化结果是否合法。这样拆分后，动作定义不需要携带任何隔离级别相关的假设；新增或调整某一隔离级别只需要修改对应公理，无须触碰动作流程。后续两节分别围绕这两步展开。

\section{公理和隔离级别约束}

沿用上节的状态机分层与``动作—公理''分工，本节先给出状态变量与三类核心动作的形式化定义，再把抽象执行 $\mathcal{X} = (\mathcal{H}, \text{VIS}, \text{AR})$ 中的可见性与仲裁关系落成可检查的逻辑表达，最后把一致性公理与隔离级别约束写成 TLC 可判定的断言。
\subsection{状态变量与动作定义}
\subsubsection{状态模型}

上节给出了变量的分层视图，这里把每个变量的具体形式写明。每个变量在 \TLAPlus 中是一个以事务标识、键或会话标识为索引的映射或集合，类型签名由 $Init$ 中的初值固定。表~\ref{tab:tla-state-vars} 列出 \texttt{MixedIsolation} 模块中全部状态变量及其语义角色。

\begin{table}[htbp]
    \centering
    \caption{\texttt{MixedIsolation} 模块状态变量一览}
    \label{tab:tla-state-vars}
    \footnotesize
    \renewcommand{\arraystretch}{1.1}
    \begin{tabularx}{\textwidth}{lX}
        \toprule
        变量                            & 索引 $\to$ 值与含义                                                                      \\
        \midrule
        \texttt{store}                & $Keys \to$ 版本集合，每个版本含 $\langle val, cts, writer \rangle$                           \\
        \texttt{tx\_buffer}           & $(AllTxns, Keys) \to$ 该事务尚未提交的写入序列                                                 \\
        \texttt{tx\_snapshot}         & $AllTxns \to$ $StartTx$ 阶段绑定的快照时间戳                                                 \\
        \texttt{tx\_checkset}         & $AllTxns \to$ 提交时需校验是否被并发写入的键集合                                                    \\
        \texttt{op\_seq}              & $TxnIds \to$ 按顺序记录的读写动作序列，公理判定的直接输入                                                \\
        \texttt{tx\_status}           & $AllTxns \to \{\text{Inactive}, \text{Active}, \text{Committed}, \text{Aborted}\}$ \\
        \texttt{tx\_commit\_time}     & $AllTxns \to$ 提交成功时由全局时钟分配的时间戳                                                     \\
        \texttt{shard\_ct}            & 全局提交计数器，提交后单调递增，作为新版本时间戳来源                                                         \\
        \texttt{client\_dep}          & $Sessions \to$ 会话观察到的最大提交时间戳，供 CC、PSI 生成快照                                         \\
        \texttt{client\_last\_commit} & $Sessions \to$ 会话最近提交时间戳，专供 RA 生成快照                                                \\
        \texttt{pc}                   & $TxnIds \to$ 已执行到操作序列的步数                                                           \\
        \bottomrule
    \end{tabularx}
\end{table}

$Init$ 中各变量按类型初始化为空映射、空集合或零值。\texttt{store} 是个例外：每个键都预置一条由 $InitTx$ 写入、时间戳为 $0$ 的 $InitVal$ 版本，使任何读操作都能拿到一个最早版本，不会出现空读。\texttt{tx\_checkset} 的填充时机由隔离级别决定：SER 在读取时纳入读集，PSI/SI 在写入时纳入写集，这一差异是 $CommitTx$ 中冲突检测分支的入口。事务级与会话级变量在第一次事务动作之前保持空状态。这一组变量不依赖任何业务字段，已经能够承载事务生命周期中的全部可观测行为；后续三类动作只需在它们之上做一阶变换。

\subsubsection{核心状态转换动作 (Transition Actions)}
操作语义的核心逻辑可归纳为三个主要的 \TLAPlus 动作定义，分别对应事务开始、操作执行与事务提交三个阶段。三类动作并不是彼此孤立的局部规则，而是共同构成了事务从建立可见快照、逐步消费操作序列，到最终完成校验与提交的完整状态演化过程。按照这一顺序展开说明，不仅能够更清楚地呈现模型中的状态转移链条，也有助于解释这些动作如何共同支撑后续的一致性公理验证。三类动作的完整 \TLAPlus 源码（经 \texttt{tla2tex} 数学体排版）见附录~\ref{appendix:tla-actions}。

\TopicTitle{A. 事务开始 ($StartTx(t)$)}

$StartTx(t)$ 形式化描述了事务获取读取视图（Read View）的过程。该动作首先根据事务的隔离级别 $Workload[t].level$ 为 $tx\_snapshot[t]$ 赋值，再将事务状态 $tx\_status$ 更新为 ``Active''，从而使事务进入可执行状态。不同隔离级别对应不同的快照来源：SI（Snapshot Isolation）直接读取当前全局提交时钟 $shard\_ct$，RA（Read Atomicity）则继承客户端最近一次提交时间 $client\_last\_commit$。更一般地说，PC、SI 和 SER 从全局提交时钟获取一致快照，CC 与 PSI 继承会话依赖，而 RA 仅依赖客户端最近一次提交时间。由此可见，事务在开始阶段绑定的快照来源不同，后续读操作可访问的版本边界也随之不同。换言之，不同隔离级别在可见性上的差异，在模型中首先体现为事务启动阶段对快照来源的不同选择。

\TopicTitle{B. 执行操作 ($ExecuteOp(t)$)}

$ExecuteOp(t)$ 给出了读写操作的具体语义，并依据 $Workload$ 中预定义的操作类型进行分支。对于读操作，模型首先检查本地写缓冲 $tx\_buffer[t][op.key]$，从而保证事务内部满足 Read-Your-Own-Writes 语义；若本地不存在对应写入，则调用辅助算子 $MaxVersion(store[op.key], tx\_snapshot[t])$，从全局存储的版本历史中选取提交时间戳不超过事务快照的最新版本，并将读取结果追加到 $op\_seq$ 以供后续验证。对于写操作，模型并不立即修改全局 $store$，而是先将结果写入 $tx\_buffer$，同时将该写操作记录到 $op\_seq$ 中。这里需要强调的是，读操作和写操作在模型中承担的并不是对称职责：前者负责在“局部写集”和“快照可见版本”之间解析事务当前可读到的值，后者则负责积累尚未提交的局部更新，为提交阶段的原子写入预先组织好输入。这样的定义保留了两个最关键的语义点：一是事务内部的本地优先读取，二是基于快照上界的版本选择。由此，版本可见性被归约为一个可由 TLC 直接枚举和检查的选择过程，同时也使本地缓冲与全局版本历史之间的分工更加清晰。

\TopicTitle{C. 提交事务 ($CommitTx(t)$)}

$CommitTx(t)$ 则形式化描述了提交协议，其中同时包含并发控制与原子性要求。模型首先通过 $HasConflict$ 执行冲突检测，并借助谓词 $IsConcurrent(t\_other)$ 识别与当前事务并发的已提交事务；若存在其他事务的写入集与当前事务的检查集（Checkset/ReadSet）发生交叉，则说明乐观并发控制（OCC）的验证失败，此时事务状态被更新为 ``Aborted''。若冲突检测通过，模型便进入提交路径：一方面递增全局时间戳 $shard\_ct$，另一方面将 $tx\_buffer$ 中的数据以原子方式合并到全局 $store$，生成带有新时间戳的版本，并同步更新客户端依赖 $client\_dep$。这一设计的关键之处在于，它将“是否允许事务提交”和“提交后如何改变全局状态”放在同一个动作中统一处理，从而避免把语义上应当同时成立的步骤拆散为多个松散的状态变化。在这一阶段，模型通过 \texttt{HasConflict} 谓词统一表达 PSI、SI 与 SER 等隔离级别所需的并发校验，并以单个状态跃迁完成提交时间分配与新版本写入。由于 \TLAPlus 以状态转移为核心，上述定义能够将“冲突检测”与“原子提交”纳入同一条可验证路径，从而避免在正文中展开与证明主线关系不大的工程性细节，也使提交阶段的形式化表达更接近实际系统中的整体提交语义。

\subsection{抽象执行关系与公理约束}

抽象执行 $\mathcal{X} = (\mathcal{H}, \text{VIS}, \text{AR})$ 中，$\mathcal{H}$ 表示操作历史。由于状态模型部分已经涉及 $\mathcal{H}$ 的基本内容，本节不再单独展开，而是重点构建 $\text{VIS}$ 与 $\text{AR}$ 的形式化表达。图~\ref{fig:tla-visar-code} 展示了这两个关系的形式化表示方式。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/VisAndAr_cropped.pdf}
    \caption{关系 VIS 和 AR 的形式化表达}
    \label{fig:tla-visar-code}
\end{figure}

这里的核心思想是将事务历史中的可见性关系与仲裁关系明确写成可检验的形式化对象，使前文给出的一致性公理不再停留于抽象的数学关系层面，而能够与模型检查中的状态约束形成一一对应。

在定义抽象执行 $\mathcal{X}$ 之后，还需要进一步对公式~\ref{eq:consistency} 中的公理进行形式化表达，才能将前文给出的数学定义真正落实为 TLC 可检查的逻辑约束。换言之，若说 $\text{VIS}$ 与 $\text{AR}$ 给出了事务历史的结构，那么这些公理所描述的就是该结构必须满足的合法性条件。图~\ref{fig:axioms} 展示了这些公理的形式化表达；在此基础上，便可进一步定义不同隔离级别对应的约束，并把“抽象一致性要求”转化为“模型检查中的可判定性质”。

\begin{center}
    \includegraphics[width=0.82\textwidth]{figures/axioms_cropped.pdf}
    \captionof{figure}{公理的形式化表达}
    \label{fig:axioms}
\end{center}


图~\ref{fig:axioms} 中各项公理并不是彼此孤立的局部条件，而是与前述的 $\text{VIS}$ 和 $\text{AR}$ 关系共同构成完整的抽象执行约束。也正因为如此，模型验证并不是单纯检查某个局部谓词是否成立，而是检查整个执行历史是否同时满足可见性关系、仲裁顺序以及一致性公理之间的联合约束。

不同于面向具体业务逻辑的程序语言，\TLAPlus 更关注状态转移结果本身。运行 \TLAPlus 代码时，TLC 模型检查器会穷举所有可能的状态转移路径，并检查这些路径是否满足预先定义的安全属性。通过配置不同的安全属性，便可以验证系统在不同隔离级别下是否满足相应约束，从而验证模型设计的正确性。

\section{验证工作负载与测试用例}
\label{sec:tla-workloads}

测试用例通过 \texttt{INSTANCE MixedIsolation WITH ...} 语法注入到 \texttt{MixedIsolation} 模型，验证引擎即可启动运行。本文共准备 10 个工作负载，全部放在验证仓库 \texttt{mil-tla-verify} 的 \texttt{workload/} 子目录下，按用途分为三类：业务基准 3 个（SmallBank、Courseware、TPC-C），沿用经典事务处理场景，并为每个事务分配隔离级别；异常探针 6 个，针对丢失更新（Lost Update）、写偏差（G2 Write Skew）、读偏差（G-single Read Skew）、长分叉（Long Fork）、会话因果链（Causal Chain）与并发计数（Counter）等典型并发异常，各构造一组最小化负载，用于在不同隔离级别组合下显式触发或排除对应异常；综合用例 1 个（MixedLevels），让 RA、CC、PC、PSI、SI、SER 六个隔离级别同时各跑一个事务，用于检验级别交错下的整体一致性。每组负载的数据结构相同：事务类型、所属隔离级别、读写操作序列、访问键集合，从而保证 TLC 按操作语义枚举事务交错执行时输入条件统一。

表~\ref{tab:tla-workloads-summary} 列出 10 个工作负载的名称、类别、设计目的，以及 TLC 在统一环境（macOS arm64，10 工作线程，3.6\,GB JVM 堆）下穷尽枚举得到的状态空间规模与运行用时。

\begin{table}[htbp]
    \centering
    \caption{\texttt{mil-tla-verify} 仓库工作负载清单（共 10 个）}
    \label{tab:tla-workloads-summary}
    \small
    \renewcommand{\arraystretch}{1.15}
    \setlength{\tabcolsep}{6pt}
    \begin{tabular}{llrrr}
        \toprule
        负载名称        & \makecell[l]{类别   \\ 设计目的} & 生成状态 & 去重状态 & 用时 \\
        \midrule
        SmallBank   & \makecell[l]{业务基准 \\ PSI 与 SER 混合的账户转账}             & 68       & 42       & 0.31\,s \\[8pt]
        Courseware  & \makecell[l]{业务基准 \\ 两 SER 事务争抢同一选课槽位}           & 51       & 34       & 0.27\,s \\[8pt]
        TPCC        & \makecell[l]{业务基准 \\ PSI 下单事务，涉及库存、订单、余额三键} & 74       & 43       & 0.37\,s \\[8pt]
        LostUpdate  & \makecell[l]{异常探针 \\ 两 PSI 事务的并发读改写}                & 43       & 26       & 0.24\,s \\[8pt]
        WriteSkew   & \makecell[l]{异常探针 \\ 经典写偏差，验证 SER 读集追踪}          & 73       & 47       & 0.39\,s \\[8pt]
        ReadSkew    & \makecell[l]{异常探针 \\ G-single，CC 读取被原子写覆盖}         & 47       & 30       & 0.34\,s \\[8pt]
        LongFork    & \makecell[l]{异常探针 \\ 双 PSI 写者加双 PSI 读者对称分叉}      & 1\,481   & 566      & 0.32\,s \\[8pt]
        CausalChain & \makecell[l]{异常探针 \\ 同会话写依赖，跨会话 TransVis 验证}    & 317      & 160      & 0.30\,s \\[8pt]
        Counter     & \makecell[l]{异常探针 \\ 两 RA 写者并行加单 CC 读者}            & 160      & 78       & 0.27\,s \\[8pt]
        MixedLevels & \makecell[l]{综合   \\ RA/CC/PC/PSI/SI/SER 六级别同时共存}       & 694\,726 & 278\,839 & 17.4\,s \\
        \bottomrule
    \end{tabular}
\end{table}

各工作负载的规模差异源自设计取舍。异常探针把事务数与操作数压到触发对应异常所需的最小水平，状态空间普遍在数百到千余之间，亚秒内即可枚举完毕；业务基准在保留场景特征的前提下同样保持小规模；MixedLevels 让六个隔离级别同时各跑一个事务，状态空间扩大到二十多万去重态，单机用时进入秒级。10 个负载合起来覆盖了第二章定义的六个隔离级别以及它们之间常见的交错执行情况，下一节的模型检查实验即基于这一负载集合展开。

本文在后续实验中以 SmallBank 作为代表性示例，其完整 \TLAPlus 测试用例见附录~\ref{appendix:tla-smallbank}。该用例定义了两个并发事务：其中事务 $T_1$ 以 PSI 隔离级别读取 \texttt{checking} 与 \texttt{savings} 两个账户并更新储蓄账户余额，事务 $T_2$ 则以 SER 隔离级别对相同数据项执行读取与写入操作。这样一组负载既保留了 SmallBank 场景中典型的账户读写依赖，又能够显式触发跨事务可见性、检查集冲突以及不同隔离级别交织执行下的一致性约束，因此适合作为 TLC 模型检查的实例化输入。

\section{模型检查实验与结果}

验证的核心判定目标是操作语义生成的运行历史（History）是否一致。在验证过程中，TLC 模型检查器会探索所有通过 \texttt{Next} 产生的合法状态转移路径，并记录事务开始、读、写、提交和中止等操作组成的 \texttt{op\_seq}。随后，模型根据该运行历史构造抽象执行关系，包括事务可见性关系 $\mathrm{VIS}$ 与仲裁顺序 $\mathrm{AR}$，并检查安全断言 \texttt{OperationalSafety == IsConsistentExecution}。若 History 中所有事务在其对应隔离级别下均满足第二章定义的公理约束，则该 History 被判定为一致；反之，只要存在某个事务违反其隔离级别约束，TLC 即会停止搜索并输出反例路径追踪报告（Error Trace File），用于定位导致不一致的具体操作交错。

为了明确模型检查实验的配置与论证范围，本文采用表~\ref{tab:tla_verification_config} 对验证输入、判定方式和边界条件进行归纳。

\begin{table}[htbp]
    \centering
    \caption{TLA+ 模型检查实验配置与验证边界}
    \label{tab:tla_verification_config}
    \small
    \renewcommand{\arraystretch}{1.28}
    \begin{tabularx}{\textwidth}{lX}
        \toprule
        配置项    & 说明                                                                                                                                    \\
        \midrule
        工作负载来源 & 业务基准 3 个（SmallBank、Courseware、TPC-C）、异常探针 6 个（丢失更新、写偏差等最小化用例）与综合用例 1 个（MixedLevels），合计 10 个负载，完整清单见表~\ref{tab:tla-workloads-summary}。 \\
        \midrule
        负载内容   & 每组负载描述事务类型、事务隔离级别、读写操作序列以及访问键集合。                                                                                                      \\
        \midrule
        验证对象   & 由操作语义状态转移生成的运行 History，而不是单个静态事务模板。                                                                                                   \\
        \midrule
        一致性判定  & 根据 History 构造 $\mathrm{VIS}$ 与 $\mathrm{AR}$，并检查其中所有事务是否满足其隔离级别对应的公理约束。                                                               \\
        \midrule
        安全断言   & \texttt{OperationalSafety == IsConsistentExecution}。                                                                                  \\
        \midrule
        搜索范围   & 每个负载的事务数与读写操作数都压到能触发对应异常或覆盖对应业务语义的最小值，状态空间在此范围内被 TLC 穷尽枚举（实际规模见表~\ref{tab:tla-workloads-summary}）。                                    \\
        \midrule
        论证边界   & 模型检查结果说明在给定负载与搜索深度内未发现违反隔离级别公理的运行 History；该结论属于有界模型检查结果，不等同于对任意规模负载和无限长执行历史的完全证明。                                                     \\
        \bottomrule
    \end{tabularx}
\end{table}

下面以 §\ref{sec:tla-workloads} 中给出的 SmallBank 测试用例为代表，展示完整的 TLC 运行过程。图~\ref{fig:tla_bash} 是模型检查器在该负载下的终端输出，其中包含模型检查进度、状态生成数量、已发现状态数量以及最终检查结果。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/code/tla_bash.pdf}
    \caption{TLC 模型检查器的终端输出示例}
    \label{fig:tla_bash}
\end{figure}

由该结果可知，TLC 模型检查器成功完成了模型检查，且未发现任何违反安全属性的状态。这进一步表明，在 SmallBank 负载下，系统的操作语义能够保证事务满足目标隔离级别的要求。

表~\ref{tab:tla-workloads-summary} 中的另外 9 个工作负载用相同方式跑了 TLC 穷尽枚举，覆盖 RA 到 SER 六个隔离级别及其相互交错的执行情形。规模差异主要出现在 MixedLevels：六个隔离级别同时各跑一个事务，状态空间到达二十多万去重态、用时进入秒级；其他负载都在亚秒级完成。10 个工作负载在 \texttt{OperationalSafety} 断言下均未出现反例，说明本文的操作语义与公理化约束在所定义的负载范围内相互一致。



\section{本章小结}

本章围绕支持混合隔离级别机制的 \TLAPlus 建模与验证展开。首先给出了事务操作语义的 \TLAPlus 表达方式，说明状态变量与核心动作如何共同描述事务执行过程；其次构建了抽象执行关系、一致性公理与隔离级别约束的形式化断言；最后结合模型检查实验及结果分析，验证了本文所设计的操作语义能够保证事务满足目标隔离级别的要求，并为后续原型系统实现提供形式化依据。


