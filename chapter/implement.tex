\chapter{分布式键值数据库原型的架构设计与实现}
本章将基于前序章节中关于分布式数据库的建模和分析，以及经\TLAPlus 验证过的支持混合隔离级别的分布式数据库操作语义，设计并实现一个数据库原型系统，用于验证该操作语义相较于只支持单一隔离级别的操作语义在性能上的优势。

\section{原型系统概述}
前文提出的事务操作语义与并发控制机制，最终需要落到可运行的系统中才能检验其可行性。基于这一点，本文实现了一个分布式键值数据库原型。该原型由路由节点、配置节点和分片节点组成，支持多版本并发控制、跨分片事务协调以及多种隔离级别的混合执行。它并不以产品化为目标，而是作为研究载体，用于验证核心机制能否落地实现，并为后续实验提供可重复的运行环境。

具体来说，该原型主要承担三项工作：验证第三章提出的系统操作语义能否转化为可执行机制，检验第四章中的形式化约束能否与关键模块设计保持一致，以及通过实验比较不同隔离级别配置下的吞吐量与回滚率表现。基于这一定位，本章先说明系统需求与实现边界，再展开架构设计和关键模块实现。

需要说明的是，第二章和第三章中的 RA、CC、PC、PSI、SI 和 SER 构成本文统一公理化模型下的理论范围，而本章原型系统选择实现 PC、PSI、SI 和 SER 四种隔离级别，主要是出于工程验证目标与系统机制覆盖范围的考虑。RA 是公理体系中的基础级别，其核心约束主要由事务内部读写规则、已提交版本读取和会话顺序维护保证，在本文原型的 MVCC 与事务缓冲机制中已作为更高隔离级别的公共基础被隐含满足，因此未作为独立可配置级别展开实验。同时，RA 和 CC 均属于约束相对较少的弱隔离级别，能够提供的异常排除能力有限，在现实数据库系统中作为独立事务隔离选项的应用场景相对较少。特别是在本文关注的混合隔离场景下，若将大量事务下放到 RA 或单独的 CC，虽然可能进一步降低协调开销，但其语义保障过弱，难以代表工程系统中常见的正确性与性能折中。CC 强调因果依赖传播，而 PSI 已在 CC 的基础上进一步加入写冲突约束，能够同时覆盖依赖传播机制与冲突认证机制的组合效果；本文实验重点又在于比较前缀快照、写冲突认证以及可串行化检查等机制对系统性能的影响。因此，工程实现中的四种级别并不是对理论模型范围的收缩，而是在六类理论级别中选取更能体现系统协调代价差异、业务语义保障强度和实验对比价值的代表性子集。

表~\ref{tab:prototype-implementation-overview} 概括了原型系统的主要实现模块及其与前文语义设计之间的对应关系。该表并不以代码行数作为工作量证明，而是展示各模块承担的状态、接口和协议职责，便于读者判断第三章中的操作语义如何落到具体系统路径中。

\begin{table}[htbp]
      \centering
      \small
      \renewcommand{\arraystretch}{1.25}
      \caption{原型系统实现模块概览}
      \label{tab:prototype-implementation-overview}
      \begin{tabularx}{\textwidth}{p{2.2cm} p{3.0cm} p{3.2cm} X}
            \toprule
            模块          & 主要职责                       & 关键状态或接口                                                 & 与语义设计的对应关系                                \\
            \midrule
            Router      & 接收客户端请求、维护事务上下文、协调跨分片提交    & 事务 ID、隔离级别、参与分片集合、Start/Commit/Abort 请求                 & 对应全局事务生命周期管理和 2PC 协调者角色                   \\
            Shard       & 执行分片内读写、维护事务缓冲和版本链         & TxBuffer、ReadSet、WriteSet、CheckSet、Prepare/Commit/Abort & 对应分片参与者状态机、MVCC 版本可见性和提交认证                \\
            Config      & 维护分片拓扑与路由元数据，并向节点同步配置      & 分片表、路由表、节点状态、流式配置同步                                     & 对应系统拓扑和分片路由前提，不直接改变隔离级别语义                 \\
            HLC         & 为快照读取、预提交和提交版本提供时间戳        & Now/Tick、Update/Receive、WallTime、Logical                & 对应 $\text{VIS}$ 与 $\text{AR}$ 在工程实现中的时间边界 \\
            MVCC Store  & 管理键值数据的多版本读写与 Pending 版本确认 & Version Chain、snapshotTime、commitTime、Pending 标记        & 对应快照读取、事务内部一致性和跨分片提交可见性约束                 \\
            ClientPool  & 为压力测试维持事务亲和性和并发连接复用        & 事务到 Client 的绑定关系、轮询分配器                                  & 保证同一事务上下文不因路由漂移而被破坏                       \\
            Test Client & 根据负载配置生成事务并采集吞吐、回滚等指标      & worker 配置、事务模板、执行结果与指标日志                                & 为第六章实验提供可重复的负载入口                          \\
            \bottomrule
      \end{tabularx}
\end{table}

从实现边界看，当前原型真实实现了事务路由、分片执行、MVCC 版本管理、HLC 时间戳维护、跨分片 2PC、单分片 QuickCommit、ClientPool 测试客户端以及可观测性埋点；同时也保留了一些研究型简化：存储层以全内存结构为主，持久化和日志恢复仅以抽象方式处理，复制容错、在线重配置和生产级权限控制不作为本文实验对象。这些边界与第七章的未来工作相对应，也限定了第六章实验结论的适用范围。

\section{需求分析}
第五章的原型系统不再重新定义混合隔离级别的操作语义，而是面向软件系统开发过程，说明前文语义模型如何转化为一个可运行、可测试的分布式键值数据库原型。第三章已经给出了事务生命周期、版本可见性、提交认证和隔离级别差异化约束的设计，本节的需求分析主要回答工程实现需要具备哪些能力、哪些能力不属于本文原型范围，以及这些需求如何服务于第六章的实验评估。

\subsection{系统目标与实现边界}

本文原型系统的目标是构建一个能够执行混合隔离级别事务的分布式键值数据库实验平台。该平台需要支持分片存储、事务路由、多版本并发控制、跨分片原子提交以及不同隔离级别事务的并发执行，从而为后续吞吐量、回滚率和部署拓扑对比实验提供统一的运行环境。

该原型系统的定位是研究验证平台，而不是生产级数据库产品。因此，系统设计优先保证事务执行路径的完整性、隔离级别机制的可配置性和实验结果的可复现性，而不追求完整的数据库生态能力。具体而言，系统真实实现事务路由、分片执行、MVCC 版本管理、HLC 时间戳维护、跨分片两阶段提交、单分片快速提交、测试客户端和性能指标采集；同时将持久化日志恢复、副本容错、在线扩缩容、权限控制和 SQL 优化器等能力排除在本文实现范围之外。这一边界有助于将工程复杂度集中到混合隔离级别事务执行机制本身，避免无关功能干扰实验结论。

\subsection{功能需求}
从软件功能角度看，原型系统需要满足以下需求。

\begin{enumerate}
      \item \textbf{键值数据访问}：系统应提供基本的键值数据访问能力，支持读取、写入、插入和删除等操作。由于本文关注事务隔离和并发控制机制，系统不支持复杂关系查询、二级索引和查询优化等传统数据库功能。
      \item \textbf{分片式数据管理}：系统应支持将数据按照分片键划分到不同 Shard 节点，Router 节点根据元数据将客户端请求转发到目标分片，Config 节点负责维护分片拓扑、路由表和节点状态，使系统能够在多节点环境下模拟分布式数据库的基本运行方式。
      \item \textbf{事务生命周期管理}：系统应支持事务开启、事务内多次读写、事务提交和事务中止。对于只访问单个分片的事务，系统应提供低开销的单分片提交路径；对于访问多个分片的事务，系统应通过分布式提交协议保证原子性。
      \item \textbf{混合隔离级别执行}：客户端在开启事务时可以指定隔离级别，系统需要在同一运行环境中同时执行 PC、PSI、SI 和 SER 事务。这里的重点不是重新说明各隔离级别的语义定义，而是保证工程实现能够根据事务携带的隔离级别参数选择相应的快照读取、版本检查和提交认证路径。
      \item \textbf{实验负载执行}：系统应提供面向实验的测试入口。测试客户端需要能够按照配置文件生成事务负载，控制并发 worker 数量，重复执行不同隔离级别组合，并记录吞吐量、事务中止、锁等待和关键阶段耗时等指标。该需求直接服务于第六章的性能评估。
\end{enumerate}

\subsection{非功能需求}
原型系统的非功能需求主要包括可复现性、可观测性和可扩展性。

\begin{enumerate}
      \item \textbf{可复现性}：系统应能够在固定负载、固定资源限制和固定部署拓扑下重复运行实验。为此，系统通过配置文件定义测试负载，通过 Docker 容器约束节点资源，并在测试客户端中统一记录执行结果。
      \item \textbf{可观测性}：系统应能够暴露事务执行过程中的关键状态和性能指标。本文实验不仅关注最终吞吐量，也需要观察提交阶段冲突、事务回滚、锁等待和 Pending 读取拦截等现象。因此，系统需要在 Router、Shard 和测试客户端等位置采集结构化指标，为实验分析提供依据。
      \item \textbf{可扩展性}：系统组件应能够以多节点方式运行，并支持通过增加 Router 或 Shard 节点改变部署拓扑。本文并不评估生产级弹性扩缩容能力，但需要系统具备基本的多节点部署能力，以便比较单可用区和跨可用区场景下的性能差异。
\end{enumerate}

\subsection{用例分析}
系统主要包含两类使用者：普通客户端和测试客户端。普通客户端用于人工执行事务操作，验证系统能够完成基本读写、事务提交和中止等功能；测试客户端用于自动生成并发事务负载，驱动系统进入可测量的稳定运行状态。

普通客户端的核心用例包括连接 Router、开启事务、指定隔离级别、执行读写操作、提交事务和中止事务。测试客户端在此基础上进一步支持读取负载配置、创建并发 worker、维持事务亲和性、收集执行结果和输出性能指标。系统管理员相关功能仅保留为原型运行所需的配置能力，例如启动 Config 节点、加载分片拓扑和维护路由元数据，不作为本文重点展开。图~\ref{fig:usercase} 展示了系统的核心用例。

\begin{figure}[!htbp]
      \centering
      \includegraphics[width=0.75\textwidth]{figures/code/usercase.pdf}
      \caption{系统用例图}
      \label{fig:usercase}
\end{figure}

\section{系统设计}
在需求分析基础上，本节从软件架构角度说明原型系统的组成与模块职责。第三章已经完成操作语义层面的设计，因此本节不再重复讨论 PC、PSI、SI 和 SER 的理论约束，而是关注这些约束在工程系统中由哪些组件承接、组件之间如何协作，以及系统如何为第六章实验提供稳定的执行路径。

\subsection{总体架构设计}
原型系统采用 Shared-nothing 分布式架构\cite{dewitt1992parallel}，由 Client、Router、Config Server 和 Shard Server 四类组件组成。Client 负责发起事务请求；Router 作为统一访问入口，维护事务上下文并执行请求路由；Config Server 维护分片拓扑和节点元数据；Shard Server 负责实际数据存储、版本管理和局部事务执行。系统总体架构如图~\ref{fig:architecture} 所示。

\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.9\textwidth]{figures/mmd/architecture.pdf}
      \caption{系统总体架构图}
      \label{fig:architecture}
\end{figure}

Router 是事务执行的全局协调层。它不保存实际数据，而是维护活动事务的全局上下文，包括事务 ID、隔离级别、快照时间戳、参与分片集合和事务状态。客户端请求到达 Router 后，Router 根据分片键查找目标 Shard，并将事务上下文随请求一并转发。提交阶段，Router 根据参与分片数量选择提交路径：单分片事务进入 QuickCommit 路径，跨分片事务进入两阶段提交路径。

Shard Server 是事务执行的局部执行层。每个 Shard 维护一部分键空间及其多版本数据，并负责处理本分片上的读写请求。对于活动事务，Shard 按需维护局部事务缓冲区，记录读集合、写集合、检查集合以及 Pending 版本状态。读操作在 Shard 内部完成版本选择，写操作先进入事务缓冲区，提交阶段再根据隔离级别和冲突检测结果决定是否发布到 MVCC 存储中。

Config Server 是系统控制面的元数据管理组件。它维护 Router 和 Shard 的节点信息、分片路由表和集群拓扑状态。Config Server 不参与事务隔离语义判断，但为 Router 的请求路由和测试环境的部署一致性提供基础信息。本文原型中的 Config Server 采用简化实现，重点满足实验环境下的元数据同步需求。

Client 分为交互式客户端和测试客户端。交互式客户端用于手工验证系统功能；测试客户端用于自动执行实验负载。由于 Router 在本原型中维护事务上下文，同一事务的所有请求必须发送到同一个 Router，因此测试客户端还需要维护事务到连接实例的绑定关系，避免事务执行过程中发生路由漂移。

\subsection{模块划分与职责设计}

从软件模块角度看，系统可以划分为路由协调模块、分片执行模块、MVCC 存储模块、时间戳模块、配置同步模块、客户端连接池模块和指标采集模块。这样的划分不是简单对应代码目录，而是围绕事务执行过程中的工程职责展开：Router 解决“事务到哪里执行”的问题，Shard 解决“事务如何在局部执行”的问题，MVCC Store 解决“读到哪个版本”的问题，HLC 解决“不同节点如何比较时间边界”的问题，ClientPool 和指标模块则解决“实验如何稳定运行并被测量”的问题。

路由协调模块位于 Router 侧，负责接收客户端请求、创建事务上下文、记录参与分片集合，并在提交时选择 QuickCommit 或 2PC。该模块的关键设计点是事务上下文集中维护。只要事务尚未结束，Router 就需要保存该事务的隔离级别、快照时间和参与分片信息，使后续读写和提交请求能够沿用同一组全局元数据。

分片执行模块位于 Shard 侧，负责处理局部读写和提交认证。Shard 并不关心完整事务访问了多少分片，而只关注当前事务在本分片上的读写状态。通过这种设计，系统将全局协调和局部并发控制分离开来：Router 管理事务范围，Shard 管理数据版本和局部冲突。

MVCC 存储模块负责维护键到版本链的映射。每个版本记录提交时间、事务标识、数据值和状态标记。读操作根据事务快照时间选择可见版本；写操作在提交前先保存在事务缓冲区或 Pending 版本中；最终提交后，Pending 版本被确认为正式版本。该模块为不同隔离级别共享同一套版本存储基础。

HLC 时间戳模块为系统提供统一的时间比较基础。Router 在事务开始和提交阶段使用 HLC 生成快照或提交相关时间戳，Shard 在本地事件和远程消息到达时更新时钟状态。相比集中式时间戳服务，HLC 更适合本原型的分片式架构，也能减少额外中心节点对实验结果的影响。

配置同步模块负责将 Config Server 中的分片拓扑和节点状态同步到 Router。本文原型采用流式连接推送配置变更，降低 Router 感知拓扑变化的延迟。虽然本文实验不重点考察动态重配置，但该模块保证了多节点环境下路由信息的一致性。

客户端连接池模块服务于测试客户端。它将负载均衡粒度从单次请求调整为单个事务：事务开始时选择一个客户端连接实例，事务执行期间保持该绑定，事务结束后再释放绑定关系。该设计保证了高并发测试时事务上下文不会被分散到不同 Router 上。

指标采集模块分布在 Shard 和测试客户端中，负责记录事务执行结果、提交耗时、回滚次数、锁等待时间和 Pending 读取拦截等指标。该模块不改变事务执行语义，但为第六章分析不同隔离级别配置下的性能差异提供数据来源。

\subsection{数据结构设计}

原型系统的核心数据结构围绕事务上下文、事务缓冲区和版本链展开。

事务上下文由 Router 维护，主要包含事务 ID、隔离级别、快照时间戳、参与分片集合和事务状态。事务 ID 用于在 Router、Shard 和日志中关联同一事务；隔离级别用于选择读写和提交路径；快照时间戳决定事务读取历史版本的边界；参与分片集合用于提交阶段判断当前事务是单分片事务还是跨分片事务。

事务缓冲区由 Shard 按需创建，主要包含读集合、写集合、检查集合和原始版本信息。读集合用于记录事务已经读取的数据项，写集合用于保存尚未发布的更新，检查集合用于提交阶段的冲突认证，原始版本信息用于判断事务执行期间目标键是否被其他事务修改。由于并非每个事务都会访问所有分片，事务缓冲区采用局部懒加载方式，只有当事务第一次访问某个 Shard 时才创建对应状态。

版本链由 MVCC Store 维护。每个键对应一条按时间组织的版本链，版本节点包含值、提交时间戳、创建事务 ID 和状态标记。对于跨分片提交，系统在 Prepare 阶段写入 Pending 版本，在最终 Commit 阶段确认该版本；如果事务中止，则删除或忽略该 Pending 版本。Pending 状态的引入使系统能够处理 2PC 中 Prepare 与 Commit 之间的可见性窗口，避免并发读请求观察到不完整的提交结果。

\subsection{接口与通信设计}

系统内部通信基于 Protobuf 和 RPC 实现。客户端与 Router 之间的接口包括事务开始、读、写、提交和中止；Router 与 Shard 之间的接口包括局部读写、Prepare、Commit、Abort 和 QuickCommit；Config Server 与 Router 之间的接口包括路由表获取、节点注册和配置变更推送。

事务开始接口由客户端调用 Router 完成，客户端需要指定目标隔离级别，Router 返回事务 ID。读写接口携带事务 ID 和操作键值，Router 根据分片键将请求转发到目标 Shard。提交接口由 Router 统一处理，如果事务只访问一个 Shard，则调用该 Shard 的 QuickCommit；如果事务访问多个 Shard，则依次执行 Prepare 和 Commit/Abort。中止接口用于主动丢弃事务缓冲区并清理 Router 侧上下文。

通信模块采用长连接和多路复用方式降低高并发测试中的连接开销。对于事务请求，系统复用 RPC 连接传递事务上下文和 HLC 时间戳；对于配置同步，Router 与 Config Server 之间保持流式连接，使路由元数据能够及时更新。

\subsection{事务执行流程设计}

从软件执行流程看，一笔事务依次经历开始、读写、提交决策和状态清理四个阶段。

事务开始阶段，客户端向 Router 发送 Start 请求并指定隔离级别。Router 创建事务上下文，生成事务 ID 和快照时间戳，并将该上下文记录在活动事务表中。对于测试客户端，该事务同时绑定到一个固定 Client 实例，保证后续请求继续到达同一 Router。

读写执行阶段，Router 根据键的分片信息将请求转发到目标 Shard。Shard 若尚未为该事务创建局部缓冲区，则先初始化事务缓冲区；读操作优先检查本地写集合，未命中时再访问 MVCC Store；写操作进入本地写集合，不立即覆盖共享存储。该流程保证事务内部读写具有一致上下文，同时把实际版本选择留给 Shard 侧完成。

提交决策阶段，Router 根据参与分片集合选择提交路径。若事务只涉及一个 Shard，Router 调用 QuickCommit，由该 Shard 在本地完成验证、写入和提交。若事务涉及多个 Shard，Router 作为协调者执行两阶段提交：Prepare 阶段要求各参与 Shard 完成本地验证并写入 Pending 状态；若全部成功，则进入 Commit 阶段确认 Pending 版本，否则进入 Abort 阶段清理局部状态。

状态清理阶段，Router 删除活动事务上下文，Shard 删除事务缓冲区，测试客户端解除事务到连接实例的绑定。通过显式清理事务状态，系统避免长时间运行测试时积累无效上下文。

\subsection{部署与测试支撑设计}

为支持第六章实验，原型系统采用容器化部署方式。Router、Shard 和 Config Server 分别作为独立服务进程运行，测试时可以通过不同部署配置将节点放置在同一可用区或多个可用区中。测试客户端从配置文件读取负载，按 worker 数量并发执行事务，并将执行结果聚合为吞吐量、回滚率和阶段耗时等指标。

这种部署设计服务于本文实验目标：在相同代码、相同负载和相同资源限制下，只改变隔离级别配置或节点部署拓扑，观察系统性能变化。由此，第五章的系统设计与第六章的实验评估形成对应关系：第五章说明系统如何执行和记录混合隔离事务，第六章则利用这些执行路径和指标验证不同配置下的性能表现。

\section{系统实现}
前两节已经从需求和设计角度说明原型系统需要具备的能力以及各模块之间的职责划分。本节进一步进入实现层面，说明这些设计在代码中的主要落点。下文不再重复第三章的操作语义推导，而是围绕分片与分布式事务、混合逻辑时钟、通信同步、客户端机制以及可观测性等实现对象展开。

\subsection{存储分片与分布式事务}

在原型系统中，分片（Shard）既负责数据的物理存储，也承担事务执行过程中的版本管理与隔离级别约束。系统通过 \lstinline|shard| 包下的 \lstinline|Shard| 结构体封装分片状态机。本小节主要说明四部分实现：分片内事务信息的组织方式、基于 MVCC 的读写流程、两阶段提交中的可见性漏洞修复，以及单分片事务的快速提交优化\footnote{本文的代码提供在仓库 \url{https://github.com/ZhangJihua0327/let-mil-go} 中。}。

\subsubsection{事务操作信息的分层与局部化管理}
本系统采用了“全局协调计算，局部状态下推”的事务信息管理范式。在事务生命周期初始化阶段（\lstinline|TxStart|），事务的全局元数据（如事务ID、隔离级别和全局快照时间戳 \lstinline|snapshotTime|）由协调者（Router）统一获取并下发。这种设计能够让不同分片共享同一个全局时间基准，进而支持前缀一致性（PC）、快照隔离（SI）以及严格可串行化（SER）。图~\ref{fig:global-timeline} 展示了全局事务信息的分发与下推流程，协调者在事务开始时通过 RPC 将全局元数据广播至所有相关分片，确保每个参与者节点都能基于同一时间线进行版本控制和并发调度。


\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.9\textwidth]{figures/mmd/global-timeline.pdf}
      \caption{组件交互时序图}
      \label{fig:global-timeline}
\end{figure}

相对地，涉及具体分片的数据操作信息由分片内的 \lstinline|TxBufferManager| 进行局部维护。考虑到未决事务的读写集可能带来较高的内存开销，代码在此采用了懒加载（Lazy Load）机制：只有当该分片真正执行首次 \lstinline|TxRead| 或 \lstinline|TxWrite| 时，才会实例化对应事务的局部缓存（\lstinline|TxBuffer|）。这种分阶段存储方式减少了无效状态分配，也使事务状态更多保留在实际参与执行的分片上。

\subsubsection{基于MVCC引擎的多版本读写流转}
分片底层的无锁化读并发依赖于多版本并发控制（MVCC）模型。以 \lstinline|TxRead| 为例，代码在读取数据时，严格遵循“读己之所写（Read-Your-Own-Writes）”的内部一致性：系统首先探测本地事务缓冲区的 Write 集合，命中后判断记录是否带有删除标记（Deleted）。如果本地无修改记录，读请求将穿透至底层的 \lstinline|MVCCStore| 操作对象，基于给定的 \lstinline|snapshotTime| 定位并获取小于但最接近该时钟的可用历史版本。

此外，程序还为最高隔离级别（SER）引入了悲观的锁机制转换。如果事务的隔离级别为 SER，分片内部将直接通过 \lstinline|lockMgr.AcquireRead| 强制索取分布式共享锁，在读写阶段即时屏蔽幻读和写倾斜缺陷，从而保证全局事务的严格串行化调度。

\subsubsection{两阶段提交可见性漏洞(Hole)的消除}
在标准的分布式两阶段提交（2PC）协议中，由于多个参与者节点的 \lstinline|Prepare| 阶段无法做到绝对原子同频，常存在所谓的可见性漏洞（Visibility Hole）——即事务虽然已经预定将要提交，但若并发的读取操作携带了更大（或相同）的时间戳进入，可能会因为读取到旧版本而破坏线性一致性。

\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.9\textwidth]{figures/mmd/2pc-hole.pdf}
      \caption{两阶段提交可见性漏洞示意图}
      \label{fig:2pc-hole}
\end{figure}

图~\ref{fig:2pc-hole} 展示了本组件处理两阶段提交可见性漏洞的方式。系统借助未决状态（Pending Status）与时间戳推进来约束该阶段的可见性：在 \lstinline|Prepare| 阶段，系统根据混合逻辑时钟（HLC）派生本地的预备时间 \lstinline|prepareTime|。此时系统并不直接覆盖旧版本，而是调用 \lstinline|store.Put| 写入带有 Pending 标记的临时版本。该预写入过程为并发读操作提供了一个额外的判定条件：若并发读取请求的时间戳满足 $\lstinline|snapshotTime| \ge \lstinline|prepareTime|$，且遇到 Pending 标记，底层将返回 \lstinline|ErrPendingRead|，调用方随后进入等待或重试。待协调者最终作出提交决定后，\lstinline|Commit| 通过 \lstinline|store.ConfirmPending(txId, commitTime)| 将临时版本确认为正式版本。通过这一过程，预提交阶段与最终提交阶段之间的可见性得以衔接。

\subsubsection{单分片快速提交(QuickCommit)优化}
当系统规划器判定当前事务的作用域仅局限于某单一分片时，该组件会触发 \lstinline|QuickCommit| 的快速提交路径，以规避2PC的网络协商与多次RPC的延迟开销\cite{thomson2012calvin,fastcommit2}，如图~\ref{fig:quick-commit-implementation}。
\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.85\textwidth]{figures/mmd/quick-commit.pdf}
      \caption{组件交互时序图}
      \label{fig:quick-commit-implementation}
\end{figure}

在 \lstinline|QuickCommit| 的实现逻辑中，由于省略了 \lstinline|Prepare| 阶段，事务提交流程被压缩为一次单分片内的原子变更。引擎首先通过 \lstinline|clock.Tick()| 获取提交时间；随后，CAS 校验模块针对 PSI、SI 以及 SER 隔离级别执行乐观并发验证，以确认缓冲区中记录的 \lstinline|OriginalVersion| 仍为当前最新版本，避免覆盖写入。在通过版本校验并获取必要的本地写锁后，\lstinline|QuickCommit| 不再经过 Pending 预写入流程，而是将缓冲区中的数据直接以带有完整 \lstinline|commitTime| 的正式版本写入 MVCC 存储层。这样一来，校验、写入、提交与释放锁都在单一分片内完成，从而减少了单分片事务的提交路径。

\subsection{混合逻辑时钟}
在并发定序和版本可见性判断中，混合逻辑时钟（HLC）提供了统一的时间基准。本原型的 HLC 组件参考了开源项目 \lstinline|glycerine/hlc| 的实现思路，并用 Go 语言实现了一个并发安全的时钟模块。

在具体的运行层面，该时钟模块重点实现了两个用于时钟状态机推进的基石接口：
\begin{itemize}
      \item \textbf{本地时间获取（Now/Tick）}：当节点（路由或分片）内部产生本地事件，如分配事务快照时间或构建局部预备时间时，系统会调用该接口。算法读取当前主机的物理时间，并与本地维护的历史最高 HLC 时间进行比较。如果物理时钟前进，则推进 \lstinline|WallTime| 并将 \lstinline|Logical| 清零；如果检测到时钟停滞或物理时钟回退，则保持当前物理时间上界不变，并通过原子操作递增逻辑计数器，从而生成单调递增的时间戳。
      \item \textbf{远程时钟同步（Update/Receive）}：在跨节点请求中，底层 RPC 拦截器会在报文头部（Header）附带发送方当前的 HLC 时间戳。接收端在解析应用层载荷之前，先调用时钟同步逻辑，对“本地维护的当前 HLC”“报文携带的远程 HLC”以及“本机当前物理时间”三者进行比较，并据此更新本地时间状态。该过程用于在节点交互过程中维持时间顺序的一致性。
\end{itemize}

基于上述时钟组件及其在 RPC 通信过程中的配合，系统能够在分布式节点之间维持一致的时间比较基础。相比集中式发号方式，这种去中心化时钟机制避免了额外的中心协调开销，也为基于 MVCC 的快照读取以及 SER 级别下的冲突检测提供了统一的时间边界。

\subsection{通信原理与同步机制}

在分布式数据库系统中，协调者（Router）与各个分片（Shard）之间存在高频内部通信。考虑到事务执行路径对网络开销较为敏感，系统没有采用传统的 HTTP RESTful 方式，而是使用基于 \lstinline|Protobuf|（如 \lstinline|shardpb| 包所示）的 RPC 框架作为跨节点通信基础。

采用基于 \lstinline|Protobuf| 的 RPC 协议，主要是出于通信效率方面的考虑。一方面，\lstinline|Protobuf| 使用紧凑的二进制编码格式，相比 JSON 等文本协议可减少请求负载；另一方面，底层 RPC 框架依托 HTTP/2 的多路复用（Multiplexing）机制，使多个并发事务请求能够共享同一条底层 TCP 连接，从而降低连接建立与传输过程中的额外开销。

在事务处理的数据面之外，集群控制面中全局路由表、分片拓扑状态以及隔离级别元数据的同步同样至关重要。为了使分片节点能够以极低延迟感知集群状态变更，系统在配置同步链路中抛弃了传统的客户端定时轮询（Polling）模型，转而引入了基于流式 RPC（Streaming RPC）的长连接实时主动推送机制，图~\ref{fig:config-sync}展示了其时序图。

\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.82\textwidth]{figures/mmd/long-conn.pdf}
      \caption{长连接配置同步示意图}
      \label{fig:config-sync}
\end{figure}

长连接配置同步策略主要带来两方面作用。其一，当全局元数据发生变更时，Router 可以通过常驻的数据流通道主动下发增量或全量配置，从而减少轮询机制带来的同步延迟和无效探测开销。其二，该长连接通道同时承担心跳保活（KeepAlive）功能，节点间的 Ping-Pong 心跳有助于 Router 较快感知底层分片的宕机或网络异常，并据此执行路由重定向与故障摘除（Failover）。

\subsection{用户客户端和测试客户端}

用户客户端的职责相对直接。客户端连接到路由节点（Router）后，用户可以通过命令行输入数据操作与事务管理命令。这里最关键的设计点不是交互形式，而是事务亲和性：同一事务的全部操作必须落到同一个 Router，才能保证事务上下文完整。基于这一要求，客户端只在事务开始时选择一次 Router；在该事务结束之前，后续读写、提交和中止请求都沿用这条连接。事务结束后，客户端再为下一笔事务重新选择路由节点。图~\ref{fig:user-client} 给出了原型系统命令行客户端的运行界面示例。

\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.9\textwidth]{figures/code/user_client.pdf}
      \caption{原型系统 CLI 运行界面示例}
      \label{fig:user-client}
\end{figure}

图~\ref{fig:user-client} 展示了一个基于命令行交互的原型系统客户端运行截图。用户可以通过输入简单的命令来执行数据操作和事务管理，如开启事务、读写数据、提交或中止事务等。客户端会将用户输入的命令解析为相应的 RPC 请求，并发送到路由节点（Router）进行处理。
开启事务后，客户端会提示事务的ID，用户可以在事务上下文中继续执行读写操作，直到提交或中止事务。客户端还会显示操作的结果，如读取到的数据、写入是否成功、提交是否成功等。

在测试客户端（Test Client）方面，同一个事务的开启（Start）和后续操作（如 Read/Write）极有可能被路由到不同的路由节点（Router）。由于 Router 将事务上下文（活跃事务记录）维护在自身的进程内存中，这会导致后续节点因找不到事务上下文而抛出错误，破坏事务正确性。为解决该问题并满足高并发评测需求，测试客户端设计了基于 ClientPool 的连接与会话路由机制，其核心理念是将负载均衡的粒度从“每次 RPC”提升到了“每个事务”。该机制具备以下三个关键特性：

\begin{enumerate}
      \item \textbf{事务亲和性（Transaction Affinity）}：客户端在发起 Start 请求前，从池中为其分配一个固定的 Client 实例。该事务内的全部后续操作以及最终的 Commit 或 Abort，均强制通过该 Client 收发，确保了一笔事务的所有 RPC 能够一致且完整地打到同一个 Router 上。事务终结后绑定关系解除。
      \item \textbf{事务级负载均衡}：ClientPool 的分配调度利用原子计数器实现轮询逻辑，使高并发创建请求能够较均匀地分配到后端 Router。例如在 3 个 Router 和 10 个测试事务的场景下，各节点的承载比例大致为 4:3:3，从而减少明显的请求倾斜。
      \item \textbf{并发安全且无排他锁}：底层复用的 gRPC \lstinline|ClientConn| 本身支持并发安全与多路复用。因此，不同事务可以并发使用池中的同一个 Client，而不必采用“借出-上锁-归还”的排他模型。这样可以避免客户端池大小直接成为测试并发度的主要限制。
\end{enumerate}

\begin{figure}[htbp]
      \centering
      \includegraphics[width=1.0\textwidth]{figures/mmd/client-pool.pdf}
      \caption{ClientPool 机制示意图}
      \label{fig:client-pool}
\end{figure}


图~\ref{fig:client-pool} 展示了基于 ClientPool 的机制。该设计在 gRPC 连接复用的基础上兼顾了事务路由的正确性与并发测试的实际需求。

测试用例通过配置文件的方式输入，测试客户端读取测试用例后，按照用例中定义的事务操作序列和隔离级别要求，自动执行。图~\ref{fig:testcase} 展示了一个测试配置文件的示例。第一行的数字指定worker的数量，worker代表着每一个独立线程，随后定义每个worker要执行的事务的顺序和事务内部的操作细节。测试客户端会根据配置文件中的定义，自动创建相应数量的worker。每个worker会按照配置文件中定义的事务操作序列，依次执行事务的开启、读写操作以及提交或中止等步骤。在执行过程中，测试客户端会记录每个操作的结果和性能指标，以便后续分析和验证。

\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.9\textwidth]{figures/code/testcase_code.pdf}
      \caption{测试用例示例}
      \label{fig:testcase}
\end{figure}


\subsection{系统日志与操作可观测性}

分布式数据库的性能调优与架构论证高度依赖于底层运行状态的透明化。本系统在分片（Shard）组件中内置了结构化的操作日志流（\lstinline|OpLog| 与 \lstinline|NoOpLog| 的灵活切换），并在事务执行的核心路径上无侵入地埋点了多维度的时间戳与状态指标。以此为基础，系统通过暴露核心指标构建了完整的可观测性监控体系，旨在以详实的实测数据支撑并验证系统在并发调度上的架构优势。

本项目的一个核心论证目标在于“探究混合隔离级别相比单一隔离级别在执行性能上的优势”。为了使这一命题可被量化，可观测性层面进行了针对性的精细粒度设计：
系统中每一阶段的生命周期事件（包括跨分片的 \lstinline|Prepare| 和 \lstinline|Commit|，以及单分片优化的 \lstinline|QuickCommit|）均会显式记录其绑定的隔离级别（\lstinline|IsolationLevel|）元数据以及对应的操作耗时（RRT, Round-Trip Time）。不仅如此，系统还通过 \lstinline|LockManager| 精确记录了高并发场景下悲观锁（尤其是 SER 隔离级别引发的共享读锁与排他写锁）排队阶段的跨事务真实锁等待耗时（Lock Wait Time）；同时在乐观并发控制（如 SI 级别）通过版本比对的 CAS 执行域内，也对因写写冲突导致回滚（Abort Rate）和触发由于遇到 Pending 标志而被异常拦截的读取（\lstinline|ErrPendingRead| 拦截率）进行了频率统计。

通过对上述多源探针数据的采集、聚合与可视化，系统能够较细致地呈现不同并发负载下的执行过程。这些监控数据表明，在混合隔离负载下，将核心事务保持为 SER，而将后台批处理或只读分析事务放宽至 SI 或 PC 后，系统可以利用 MVCC 的历史版本读取机制减少部分由强隔离带来的锁争用与排队开销。结合基准测试结果可以看出，混合隔离配置在当前原型系统中与更高吞吐量和较低排队开销相对应。

\section{本章小结}

本章说明了原型系统的定位、需求边界以及关键实现。系统采用分布式架构，支持 PC、PSI、SI 和 SER 四种隔离级别混合运行，并围绕全内存 MVCC 存储、基于 HLC 的时间定序、跨分片事务协调和客户端路由机制给出了具体实现。该原型为下一章的系统测试提供了可运行的实验平台。
