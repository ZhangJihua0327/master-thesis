\documentclass[
    type = master,
    degree = professional,
   % anonymous,              % 开启盲审模式
    twoside,
    fontset = win
  ]{njuthesis}

\usepackage{amsmath}
\usepackage{centernot}
\usepackage{makecell}
\usepackage{float}
\usepackage{tabularx}
\usepackage{tikz}
\usepackage{algorithm}
\usepackage{etoolbox}
\usepackage[
    noEnd=true,
    indLines=true,
    beginComment={{\scriptsize$\triangleright$}~},
    beginLComment={{\scriptsize$\triangleright$}~},
    endLComment={~{\scriptsize$\triangleleft$}}
]{algpseudocodex}
\usepackage{pdfpages}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc}
\tikzset{algpxIndentLine/.style={draw=black!35, semithick}}

\input{newcommands}
\input{zhangjihua-master-thesis.def}
\usepackage{listings}

\begin{document}

\maketitle
\raggedbottom

\begin{abstract}
      分布式数据库已成为现代互联网应用迈向分布式与云原生架构的重要基础设施。面对高并发和多样化业务需求，系统设计长期需要在数据一致性与执行性能之间进行权衡：单一强隔离级别能够提供严格的正确性保障，但往往带来较高的并发控制开销；单一弱隔离级别虽然吞吐量较高，却难以满足关键事务的严格一致性要求。

      针对上述问题，本文围绕混合隔离级别事务的统一语义建模与系统实现展开研究，设计并实现了一个基于分片（Sharding）架构、支持混合隔离级别的分布式键值数据库原型。本文提出了一种面向混合隔离级别场景的并发控制协议，使事务能够根据业务需求选择不同隔离级别，并支持跨分片事务的原子执行。

      为保证协议在混合场景下的正确性，本文使用 \TLAPlus 对系统操作语义进行形式化建模，并通过模型检查验证关键安全属性；在此基础上，进一步构建了一个分布式数据库验证平台，实现了分布式事务调度、多版本并发控制（MVCC）存储引擎以及网络通信模块，用于模拟分布式环境下的事务处理流程。

      基准测试结果表明，本文设计的混合隔离级别机制能够正确执行混合事务逻辑；相较于统一采用强隔离级别的基线配置，该机制在保证目标语义成立的前提下能够稳定提升系统吞吐量并改善平均延迟。上述结果说明，本文提出的机制具备较好的工程可行性，并为在分布式数据库中兼顾正确性与性能提供了一种可行路径。
\end{abstract}

\begin{abstract*}
      Distributed databases have become essential infrastructure for modern internet applications moving toward distributed and cloud-native architectures.

      Under high concurrency and diverse workloads, system design must continually balance data consistency against execution performance:
      a single strong isolation level provides strict correctness guarantees but often incurs substantial concurrency-control overhead,
      whereas a single weak isolation level may deliver higher throughput but cannot satisfy the stringent consistency requirements of critical transactions.

      To address this problem, this thesis studies unified semantic modeling and system implementation for transactions running under mixed isolation levels.
      We design and implement a prototype distributed key-value database based on a sharding architecture that supports mixed isolation levels, and propose a concurrency control protocol tailored to such scenarios.
      The protocol allows transactions to select different isolation levels according to application requirements while supporting atomic execution of cross-shard transactions.

      To ensure correctness in mixed-isolation settings, we model the system semantics using \TLAPlus and verify key safety properties through model checking.
      Based on the verified model, we further build a distributed database validation platform that implements distributed transaction scheduling, a multi-version concurrency control (MVCC) storage engine, and network communication modules for realistic simulation of distributed transaction processing.

      Benchmark results show that the proposed mechanism executes mixed transaction logic correctly and, compared with a baseline configuration that uniformly adopts a strong isolation level, can consistently improve system throughput and reduce average latency while preserving the required semantic guarantees.
      These results demonstrate the engineering feasibility of the proposed mechanism and suggest a practical way to better balance correctness and performance in distributed database systems.
\end{abstract*}

\tableofcontents
\listoffigures
\listoftables

\mainmatter

\begin{notation}[12cm]
      \item[TLA] 时序逻辑行动 (Temporal Logic of Actions)
      \item[MVCC] 多版本并发控制 (Multi-Version Concurrency Control)
      \item[RC] 读已提交隔离级别 (Read Committed)
      \item[RA] 读原子性隔离级别 (Read Atomicity)
      \item[CC] 因果一致性隔离级别 (Causal Consistency)
      \item[PC] 前缀一致性隔离级别 (Prefix Consistency)
      \item[PSI] 并行快照隔离级别 (Parallel Snapshot Isolation)
      \item[SI] 快照隔离级别 (Snapshot Isolation)
      \item[SER] 可串行化隔离级别 (Serializable)
      \item[2PC] 两阶段提交协议 (Two-Phase Commit)
\end{notation}

\include{chapter/introduction}
\include{chapter/related-tech}
\include{chapter/operational-semantics}
\include{chapter/tla}
\include{chapter/implement}
\include{chapter/test}
\include{chapter/conclusion}

\printbibliography

\begin{acknowledgement}
      致谢TODO
\end{acknowledgement}

\appendix
\include{appendix/tlaplus}

\end{document}
